export enum PUBLIC_ROUTES {
  LOGIN = 'login',
  REGISTER = 'register',
  SHOP = 'shop',
  SINGLE_PRODUCT = 'product/:id',
}

export enum PRIVATE_ROUTES {
  BASKET = 'basket',
  WISHLIST = 'wishlist',
}
