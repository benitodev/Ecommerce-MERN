export enum PRODUCT_ENDPOINTS {
  POPULAR_PRODUCTS = '/popular',
  GET_PRODUCTS = '/:category?',
  GET_PRODUCT = '/single/:id',
  CREATE_PRODUCT = '/',
  DELETE_PRODUCT = '/',
  UPDATE_PRODUCT = '/',
}
